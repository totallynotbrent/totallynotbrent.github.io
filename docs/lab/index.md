---
title: Lab
description: Stress-testing what Quartz v5 can actually do.
tags:
  - lab
---

# Lab

Everything below is Quartz v5 doing *things*. One page per capability so you can check each in isolation.

- [[lab/mermaid]] — mermaid diagrams rendered from fenced blocks
- [[lab/katex]] — LaTeX math, inline and block
- [[lab/callouts]] — callout blocks (note, tip, warning, etc.)
- [[lab/tables]] — tables, task lists, highlights
- [[lab/code]] — syntax-highlighted code in many languages
- [[lab/map.canvas]] — a JSON Canvas page, pannable and zoomable
- [[lab/tags]] — tag pages + filtering

[[index|Back home]]