---
title: Code
description: Syntax-highlighted code blocks.
---

# Code

Syntax highlighting works across languages.

```python
def meaning_of_life(answer: int) -> bool:
    if answer == 42:
        return True
    return False
```

```rust
fn main() {
    let projects = vec!["bread", "sage", "nuFor"];
    for p in projects {
        println!("{p} is alive");
    }
}
```

```js
const build = async () => {
  const pages = await quartz.build();
  console.log(`emitted ${pages.length} pages`);
};
```

[[lab/index|Back to lab]]